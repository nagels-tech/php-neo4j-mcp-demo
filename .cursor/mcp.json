{
  "mcpServers": {
    "neo4j-mcp": {
      "command": "docker",
      "args": [
        "compose",
        "-f",
        "<path-to-docker-compose.yml>",
        "exec",
        "php",
        "bin/console",
        "mcp:server"
      ]
    }
  }
}
